name: mama_ems_inventory_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  
  # Database
  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0+4
  sqflite_common_ffi_web: ^0.4.0
  path: ^1.8.3
  
  # State Management
  provider: ^6.1.1
  
  # Date/Time utilities
  intl: ^0.19.0
  
  # UUID generation for order IDs
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true